# SPDX-License-Identifier: MIT
# A service configuration without log_level, which therefore takes the default;
# spaces around keys and values are ignored.
include = testdata/r*.conf
  output_dir=/var/spool/noeml
work_dir = /var/lib/eml-list-to-noeml
owner_user = root
owner_group = mail
