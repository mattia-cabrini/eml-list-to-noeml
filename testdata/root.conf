# SPDX-License-Identifier: MIT
# A complete mailbox configuration. It names no work_dir, so the envelopes are
# built in the service one.
file = testdata/sample.mbox
recipient = admin@example.com
gpg_key = admin@example.com
gpg_passphrase_file = testdata/root.passphrase
